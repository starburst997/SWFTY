#!/bin/bash

# cd to the script's directory so we can run it from anywhere
cd "$(dirname "$0")"

#live-server bin/html5/bin --watch=SWFTY.js --wait=750

# Older OpenFL
live-server bin/html5/debug/bin --watch=SWFTY.js --wait=750